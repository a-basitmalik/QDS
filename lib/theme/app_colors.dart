import 'package:flutter/material.dart';

class AppColors {
  // Base
  static const bg = Color(0xFFF5F5F7);
  static const panelGrey = Color(0xFFEFEFF2);
  static const surface = Colors.white;

  // Text
  static const textDark = Color(0xFF1C1C1E);
  static const textMid = Color(0xFF6B6B70);
  static const textSoft = Color(0xFF8A8A90);

  // Lines / Borders
  static const divider = Color(0xFFE9E9EE);
  static const chipFill = Color(0xFFEDEDF2);

  // Dark capsule
  static const pill = Color(0xFF0F0F12);

  // Neutrals
  static const shadow = Color(0x12000000);
}
